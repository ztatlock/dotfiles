SHELL=/bin/bash
LOCAL_LOADS=/home/ztatlock/loads
HERBIE=/home/ztatlock/herbie

30 2 * * * $HERBIE/infra/run.sh

*/5 * * * * source $HOME/.bashrc; $HOME/bin/update-load.sh -l $LOCAL_LOADS/ 2> /dev/null

# NOTE: moved reincarnate nightlies to caribou
# REINCARNATE=/home/ztatlock/reincarnate
# 0  0 * * * cd $HOME/reincarnate && git pull --quiet
# 30 0 * * * $HOME/reincarnate/src/script/nightly.sh -c medium -j 4
